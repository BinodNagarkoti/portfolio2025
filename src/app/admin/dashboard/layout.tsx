'use client';

// This file has been moved to /src/app/(admin)/admin/dashboard/layout.tsx
// and can be safely deleted. By removing the default export, we prevent a route conflict.

function DeprecatedDashboardLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
